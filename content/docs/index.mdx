---
title: Overview & Quickstart
description: Production-grade multidimensional evaluation toolkit for AI image generation
---

# Overview & Quickstart

`image-evaluator` is a production-grade, lightweight evaluation toolkit designed for generative AI researchers and practitioners. Under a unified interface, it systematically covers **nine core evaluation dimensions**: single-image aesthetic score, cross-modal text-image semantic alignment, subject facial identity fidelity, pairwise fidelity triad (deep perceptual distance, structural similarity, and peak signal-to-noise ratio), dataset generative distribution distance (Fréchet Inception Distance and Kernel Inception Distance), and human preference alignment.

---

## Metric Architecture Matrix

The toolkit structures its nine core metrics across four functional dimensions:

| Dimension | Metric | Backbone & Feature Space | Direction | Documentation |
| :--- | :--- | :--- | :--- | :--- |
| **Perceptual Quality** | `aesthetic` | OpenCLIP `ViT-L-14` + linear regression head | Higher is better | [Aesthetic Score](/single-image/aesthetic) |
| **Text-Image Alignment** | `clip` | CLIP `clip-vit-base-patch32` cosine similarity | Higher is better | [CLIP Score](/single-image/clip) |
| **Identity Fidelity** | `arcface` | InsightFace `buffalo_l` 512-dim cosine distance | Lower is better | [ArcFace Distance](/single-image/arcface) |
| **Perceptual Distance** | `lpips` | AlexNet multi-scale deep activation features | Lower is better | [LPIPS Distance](/pairwise/lpips) |
| **Structural Statistics** | `ssim` | $11 \times 11$ Gaussian sliding window covariance | Higher is better | [SSIM Similarity](/pairwise/ssim) |
| **Pixel Reconstruction** | `psnr` | Pixel-wise Mean Squared Error logarithmic ratio | Higher is better | [PSNR Ratio](/pairwise/psnr) |
| **Distribution Distance** | `fid` | Inception-v3 pool3 Gaussian quadratic distance | Lower is better | [FID Score](/distribution/fid) |
| **Kernel MMD U-Statistic** | `kid` | Polynomial kernel maximum mean discrepancy | Lower is better | [KID Score](/distribution/kid) |
| **Human Preference** | `pickscore` | Fine-tuned CLIP ViT-H-14 on Pick-a-Pic data | Higher is better | [PickScore](/preference/pickscore) |

---

## Core Capability Highlights

<Cards>
  <Card title="Single-Image & Cross-Modal" href="/single-image/aesthetic" description="Evaluate standalone aesthetic appeal, prompt compliance, and facial identity preservation." />
  <Card title="Pairwise Fidelity Triad" href="/pairwise/lpips" description="Decouple deep perceptual texture (LPIPS), structural integrity (SSIM), and pixel MSE (PSNR)." />
  <Card title="Generative Distribution" href="/distribution/fid" description="Measure generative dataset distribution shift via large-sample FID and unbiased small-sample KID." />
  <Card title="Human Preference Alignment" href="/preference/pickscore" description="Score synthetic outputs using large-scale empirical human choice models." />
  <Card title="Interactive Benchmark" href="/benchmark/visual-demo" description="Explore interactive split-screen comparisons across extreme distortion and color-shift cases." />
</Cards>

---

## Quickstart

<Steps>
  <Step>
    ### Environment Installation

    Install `image-evaluator` in your Python 3.11+ environment:

    ```bash
    pip install image-evaluator
    ```
  </Step>

  <Step>
    ### Single-Image Aesthetic Evaluation

    Score standalone visual appeal using LAION aesthetic predictor:

    <Tabs items={["CLI Command", "Python API"]}>
      <Tab value="CLI Command">
        ```bash
        image-evaluator --metrics aesthetic --image path/to/sample.png
        ```
      </Tab>
      <Tab value="Python API">
        ```python
        from image_evaluator.laion_ai_aesthetic_predictor import LaionAiAestheticPredictor

        predictor = LaionAiAestheticPredictor()
        score = predictor.evaluate_aesthetic_score("path/to/sample.png")
        print(f"Aesthetic Score: {score:.4f}")
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    ### Text-Image Alignment & Preference Scoring

    Verify text prompt alignment and subjective human preference simultaneously:

    <Tabs items={["CLI Command", "Python API"]}>
      <Tab value="CLI Command">
        ```bash
        image-evaluator --metrics clip pickscore \
            --image path/to/sample.png \
            --prompt "an astronaut riding a horse on mars"
        ```
      </Tab>
      <Tab value="Python API">
        ```python
        from image_evaluator.clip_score_predictor import ClipScorePredictor
        from image_evaluator.pickscore_predictor import PickScorePredictor

        clip_pred = ClipScorePredictor()
        pick_pred = PickScorePredictor()

        prompt = "an astronaut riding a horse on mars"
        clip_val = clip_pred.evaluate_clip_score("path/to/sample.png", prompt=prompt)
        pick_val = pick_pred.evaluate_pickscore("path/to/sample.png", prompt=prompt)

        print(f"CLIP Score: {clip_val:.4f}, PickScore: {pick_val:.4f}")
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    ### Pairwise Fidelity Triad Evaluation

    Quantify perceptual, structural, and numerical differences against a reference ground truth:

    <Tabs items={["CLI Command", "Python API"]}>
      <Tab value="CLI Command">
        ```bash
        image-evaluator --metrics lpips ssim psnr \
            --reference path/to/reference.png \
            --image path/to/generated.png
        ```
      </Tab>
      <Tab value="Python API">
        ```python
        from image_evaluator.lpips_predictor import LPIPSPredictor
        from image_evaluator.ssim_predictor import SSIMPredictor
        from image_evaluator.psnr_predictor import PSNRPredictor

        lpips_p = LPIPSPredictor()
        ssim_p = SSIMPredictor()
        psnr_p = PSNRPredictor()

        ref, gen = "path/to/reference.png", "path/to/generated.png"
        print(f"LPIPS: {lpips_p.evaluate_lpips(ref, gen):.4f}")
        print(f"SSIM:  {ssim_p.evaluate_ssim(ref, gen):.4f}")
        print(f"PSNR:  {psnr_p.evaluate_psnr(ref, gen):.2f} dB")
        ```
      </Tab>
    </Tabs>
  </Step>

  <Step>
    ### Generative Dataset Distribution Distances

    Compare feature distribution shifts between generated and ground-truth directories:

    <Tabs items={["CLI Command", "Python API"]}>
      <Tab value="CLI Command">
        ```bash
        image-evaluator --metrics fid kid \
            --reference path/to/real_folder/ \
            --image path/to/generated_folder/
        ```
      </Tab>
      <Tab value="Python API">
        ```python
        from image_evaluator.fid_predictor import FIDPredictor
        from image_evaluator.kid_predictor import KIDPredictor

        fid_p = FIDPredictor()
        kid_p = KIDPredictor()

        real_dir, gen_dir = "path/to/real_folder/", "path/to/generated_folder/"
        print(f"FID: {fid_p.evaluate_fid(real_dir, gen_dir):.4f}")

        mean_kid, std_kid = kid_p.evaluate_kid(real_dir, gen_dir)
        print(f"KID: {mean_kid:.6f} (+/- {std_kid:.6f})")
        ```
      </Tab>
    </Tabs>
  </Step>
</Steps>

---

## Architectural Principles

<Accordions>
  <Accordion title="Lazy Loading & Zero Memory Leakage">
    The engine initializes only the models explicitly declared in `--metrics`. Weight caches and intermediate tensors are strictly scoped. Unselected heavy models (such as InsightFace or PickScore ViT-H-14) remain completely unloaded, preserving host RAM and GPU VRAM during long-running pipelines.
  </Accordion>

  <Accordion title="Strict Spatial Alignment & Fail-Fast Safety">
    Pairwise metrics enforce an absolute dimension check ($H_{\text{ref}} = H_{\text{gen}}$ and $W_{\text{ref}} = W_{\text{gen}}$). The library strictly rejects silent interpolation or resizing, guaranteeing that resolution scaling artifacts never contaminate deep perceptual measurements.
  </Accordion>

  <Accordion title="Hardware Acceleration & Cross-Platform Support">
    Native support across Linux (CUDA acceleration, CPU fallback) and macOS (Apple Silicon Metal MPS, CPU). Automatic device resolution dynamically optimizes batch throughput without manual torch device configuration.
  </Accordion>
</Accordions>
